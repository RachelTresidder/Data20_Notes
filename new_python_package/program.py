from app.fizzbuzz import fizzbuzz_function

# so the folder.file name, then import the class or function name

# if in class, need to quote an instance of it
#   e.g. example = fizzbuzz(argument)

# if a function

print(fizzbuzz_function(19))
# i need to insert a number as I have not used a range/the function requires a number
