from setuptools import setup

setup(name='app')
# passing in the package name = app

# can include more info, and should:
#   version
#   description
#   author email
#   author name
#   github link
#   etc....

# now need to use pip to install or package
# open the terminal and type 'pip install .'
#   the '.' will install everything

# once the pip install . is done, this file can be closed